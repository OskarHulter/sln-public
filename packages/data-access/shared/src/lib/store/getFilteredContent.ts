import { Content } from './store.js'

export default function getFilteredContent(content: Content) {
  return content
  //   .filter((d: ContentKeys) => {
  //   if (options.filter === 'SHOW_ALL') {
  //     return true
  //   } else return d === filter.toLowerCase()
  // })
}
