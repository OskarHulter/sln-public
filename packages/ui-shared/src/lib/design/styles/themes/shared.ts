import { Theme } from '@nextui-org/react'
import { fonts } from '../../assets/fonts'
import { colors } from '../../colors'


export const sharedTheme: Theme = {
  theme: {
    colors,
    fonts,
  },
}
